# Pizazz Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a Casino Elegance title screen, Web Audio API sound system, confetti burst on win, and a dramatic dealer hole card reveal to the Blackjack game.

**Architecture:** `sound.js` is a self-contained `SoundSystem` class loaded before `ui.js`. `index.html` gets static markup for the title screen and a mute button in the top bar. `ui.js` gains `showTitleScreen()` (replaces the `renderBetting()` boot call), `toggleMute()`, `triggerConfetti()`, and sound/effect calls at the right event points. `game.js` is untouched.

**Tech Stack:** Vanilla JS, Web Audio API (no external audio files), CSS animations, localStorage for mute persistence.

**Note on testing:** The existing test suite (`node tests/game.test.js`) covers `game.js` only — DOM and audio changes can't be unit-tested there. Each task includes a browser verification step instead. Run `node tests/game.test.js` at the end to confirm nothing regressed.

---

### Task 1: Create sound.js

**Files:**
- Create: `sound.js`

- [ ] **Step 1: Create sound.js with the complete SoundSystem class**

```js
class SoundSystem {
  constructor() {
    this._audioCtx = null;
    this._ambientNodes = null;
    this._muted = localStorage.getItem('bj_muted') === 'true';
  }

  get muted() { return this._muted; }

  setMuted(val) {
    this._muted = val;
    localStorage.setItem('bj_muted', String(val));
    if (val) this.stopAmbient();
  }

  _ctx() {
    if (!this._audioCtx) {
      this._audioCtx = new (window.AudioContext || window.webkitAudioContext)();
    }
    if (this._audioCtx.state === 'suspended') this._audioCtx.resume();
    return this._audioCtx;
  }

  playChip() {
    if (this._muted) return;
    const ctx = this._ctx();
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();
    osc.connect(gain);
    gain.connect(ctx.destination);
    osc.type = 'sine';
    osc.frequency.value = 1200;
    gain.gain.setValueAtTime(0.3, ctx.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.08);
    osc.start(ctx.currentTime);
    osc.stop(ctx.currentTime + 0.08);
  }

  playDeal() {
    if (this._muted) return;
    const ctx = this._ctx();
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();
    osc.connect(gain);
    gain.connect(ctx.destination);
    osc.type = 'sine';
    osc.frequency.setValueAtTime(400, ctx.currentTime);
    osc.frequency.exponentialRampToValueAtTime(200, ctx.currentTime + 0.15);
    gain.gain.setValueAtTime(0.15, ctx.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.15);
    osc.start(ctx.currentTime);
    osc.stop(ctx.currentTime + 0.15);
  }

  playFlip() {
    if (this._muted) return;
    const ctx = this._ctx();
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();
    osc.connect(gain);
    gain.connect(ctx.destination);
    osc.type = 'sine';
    osc.frequency.setValueAtTime(300, ctx.currentTime);
    osc.frequency.exponentialRampToValueAtTime(600, ctx.currentTime + 0.2);
    gain.gain.setValueAtTime(0.2, ctx.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.2);
    osc.start(ctx.currentTime);
    osc.stop(ctx.currentTime + 0.2);
  }

  playWin() {
    if (this._muted) return;
    const ctx = this._ctx();
    [523, 659, 784].forEach((freq, i) => {
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.connect(gain);
      gain.connect(ctx.destination);
      osc.type = 'sine';
      osc.frequency.value = freq;
      const t = ctx.currentTime + i * 0.15;
      gain.gain.setValueAtTime(0, t);
      gain.gain.linearRampToValueAtTime(0.3, t + 0.02);
      gain.gain.exponentialRampToValueAtTime(0.001, t + 0.4);
      osc.start(t);
      osc.stop(t + 0.4);
    });
  }

  playBlackjack() {
    if (this._muted) return;
    const ctx = this._ctx();
    [523, 659, 784, 1047].forEach((freq, i) => {
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.connect(gain);
      gain.connect(ctx.destination);
      osc.type = 'sine';
      osc.frequency.value = freq;
      const t = ctx.currentTime + i * 0.18;
      gain.gain.setValueAtTime(0, t);
      gain.gain.linearRampToValueAtTime(0.4, t + 0.02);
      gain.gain.exponentialRampToValueAtTime(0.001, t + 0.5);
      osc.start(t);
      osc.stop(t + 0.5);
    });
  }

  playLose() {
    if (this._muted) return;
    const ctx = this._ctx();
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();
    osc.connect(gain);
    gain.connect(ctx.destination);
    osc.type = 'sine';
    osc.frequency.setValueAtTime(300, ctx.currentTime);
    osc.frequency.exponentialRampToValueAtTime(150, ctx.currentTime + 0.4);
    gain.gain.setValueAtTime(0.3, ctx.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.4);
    osc.start(ctx.currentTime);
    osc.stop(ctx.currentTime + 0.4);
  }

  playPush() {
    if (this._muted) return;
    const ctx = this._ctx();
    const osc = ctx.createOscillator();
    const gain = ctx.createGain();
    osc.connect(gain);
    gain.connect(ctx.destination);
    osc.type = 'sine';
    osc.frequency.value = 440;
    gain.gain.setValueAtTime(0.2, ctx.currentTime);
    gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.3);
    osc.start(ctx.currentTime);
    osc.stop(ctx.currentTime + 0.3);
  }

  startAmbient() {
    if (this._muted || this._ambientNodes) return;
    const ctx = this._ctx();
    this._ambientNodes = [110, 165, 220].map(freq => {
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.connect(gain);
      gain.connect(ctx.destination);
      osc.type = 'sine';
      osc.frequency.value = freq;
      gain.gain.value = 0.03;
      osc.start();
      return { osc, gain };
    });
  }

  stopAmbient() {
    if (!this._ambientNodes) return;
    const ctx = this._ctx();
    this._ambientNodes.forEach(({ osc, gain }) => {
      gain.gain.exponentialRampToValueAtTime(0.001, ctx.currentTime + 0.5);
      osc.stop(ctx.currentTime + 0.5);
    });
    this._ambientNodes = null;
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add sound.js
git commit -m "feat: add SoundSystem class with Web Audio API sounds and ambient music"
```

---

### Task 2: Update index.html

**Files:**
- Modify: `index.html`

- [ ] **Step 1: Add `sound.js` script tag, `#title-screen` overlay, and mute button to top bar**

Replace the entire `index.html` with:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blackjack</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div id="title-screen">
    <div class="title-suit-row">♠ &nbsp; ♥ &nbsp; ♦ &nbsp; ♣</div>
    <div class="title-heading">BLACKJACK</div>
    <div class="title-tagline">Beat the Dealer</div>
    <button id="playNowBtn" class="btn-action btn-outline title-play-btn">Play Now</button>
    <button id="titleMuteBtn" class="btn-mute">🔊</button>
  </div>

  <div id="app">
    <header class="top-bar">
      <span class="game-title">♠ BLACKJACK ♠</span>
      <div class="balance-area">
        <span class="balance-label">Balance</span>
        <span id="balance" class="balance-amount">$500</span>
        <button id="addFunds" class="btn-add-funds">+ ADD FUNDS</button>
        <button id="gameMuteBtn" class="btn-mute">🔊</button>
      </div>
    </header>

    <main class="table">
      <section class="dealer-area">
        <div id="dealer-label" class="hand-label">Dealer</div>
        <div id="dealer-cards" class="cards-row"></div>
      </section>

      <div class="table-divider"></div>

      <section id="player-area" class="player-area"></section>
    </main>

    <footer id="action-bar" class="action-bar"></footer>
  </div>

  <div id="result-overlay" class="result-overlay"></div>

  <script src="sound.js"></script>
  <script src="game.js"></script>
  <script src="ui.js"></script>
</body>
</html>
```

- [ ] **Step 2: Open index.html in a browser**

Verify the title screen is visible and covers the game. The game elements underneath should not be interactable. Nothing needs to work yet — just confirm the markup renders without console errors.

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "feat: add title screen markup and mute button to HTML"
```

---

### Task 3: Add CSS for title screen, confetti, and dealer reveal

**Files:**
- Modify: `style.css`

- [ ] **Step 1: Append title screen styles to style.css**

Add at the bottom of `style.css`:

```css
/* ============================================================
   Title screen
   ============================================================ */
#title-screen {
  position: fixed;
  inset: 0;
  z-index: 200;
  background: radial-gradient(ellipse at center, #1a6b3a 0%, #0d4a26 50%, #082e18 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  transition: opacity 0.6s ease;
}

#title-screen::before {
  content: '';
  position: absolute;
  inset: 0;
  background: repeating-linear-gradient(
    90deg,
    transparent,
    transparent 3px,
    rgba(255, 255, 255, 0.015) 3px,
    rgba(255, 255, 255, 0.015) 4px
  );
  pointer-events: none;
}

#title-screen::after {
  content: '';
  position: absolute;
  inset: 12px;
  border: 1px solid rgba(201, 168, 76, 0.15);
  border-radius: 8px;
  pointer-events: none;
}

.title-suit-row {
  font-size: 13px;
  letter-spacing: 8px;
  color: rgba(201, 168, 76, 0.7);
  font-weight: 700;
  margin-bottom: 10px;
  position: relative;
  z-index: 1;
}

.title-heading {
  font-size: clamp(36px, 8vw, 64px);
  font-weight: 900;
  color: #c9a84c;
  letter-spacing: 8px;
  text-shadow: 0 0 30px rgba(201, 168, 76, 0.5), 0 2px 8px rgba(0, 0, 0, 0.6);
  margin-bottom: 8px;
  position: relative;
  z-index: 1;
}

.title-tagline {
  font-size: 11px;
  letter-spacing: 4px;
  color: rgba(255, 255, 255, 0.35);
  text-transform: uppercase;
  margin-bottom: 32px;
  position: relative;
  z-index: 1;
}

.title-play-btn {
  font-size: 13px;
  letter-spacing: 3px;
  position: relative;
  z-index: 1;
}

/* ============================================================
   Mute button (title screen + top bar)
   ============================================================ */
.btn-mute {
  background: transparent;
  border: none;
  font-size: 18px;
  cursor: pointer;
  padding: 6px;
  opacity: 0.5;
  transition: opacity 0.15s;
  line-height: 1;
}

.btn-mute:hover { opacity: 1; }

#title-screen .btn-mute {
  position: absolute;
  bottom: 14px;
  right: 16px;
  z-index: 1;
}

/* ============================================================
   Confetti
   ============================================================ */
@keyframes confettiFall {
  0%   { transform: translateY(-10px) rotate(0deg); opacity: 1; }
  100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
}

.confetti-particle {
  position: fixed;
  top: 0;
  width: 8px;
  height: 8px;
  border-radius: 2px;
  pointer-events: none;
  z-index: 300;
  animation: confettiFall linear forwards;
}

/* ============================================================
   Dealer reveal pulse
   ============================================================ */
@keyframes dealerRevealPulse {
  0%   { transform: scale(1); }
  50%  { transform: scale(1.03); }
  100% { transform: scale(1); }
}

.dealer-area--reveal {
  animation: dealerRevealPulse 0.3s ease;
}
```

- [ ] **Step 2: Open index.html in a browser**

Confirm the title screen looks correct: dark green felt background, gold inner border ring, gold suit row, BLACKJACK heading with glow, "Beat the Dealer" tagline, Play Now button. Mute icon bottom-right. No console errors.

- [ ] **Step 3: Commit**

```bash
git add style.css
git commit -m "feat: add title screen, confetti, and dealer reveal CSS"
```

---

### Task 4: Implement title screen logic in ui.js

**Files:**
- Modify: `ui.js`

- [ ] **Step 1: Add `sound`, `showTitleScreen`, `toggleMute`, and `updateMuteButtons` to ui.js**

Insert these lines at the very top of `ui.js`, before `const game = new BlackjackGame();`:

```js
const sound = new SoundSystem();
```

Then add these three functions anywhere before the `// Boot` section at the bottom of `ui.js`:

```js
// ============================================================
// Title screen
// ============================================================
function showTitleScreen() {
  const screen    = document.getElementById('title-screen');
  const playBtn   = document.getElementById('playNowBtn');
  const titleMute = document.getElementById('titleMuteBtn');

  updateMuteButtons();

  playBtn.addEventListener('click', () => {
    sound.startAmbient();
    screen.style.opacity = '0';
    screen.addEventListener('transitionend', () => {
      screen.remove();
      renderBetting();
    }, { once: true });
  });

  titleMute.addEventListener('click', toggleMute);
}

function toggleMute() {
  sound.setMuted(!sound.muted);
  updateMuteButtons();
}

function updateMuteButtons() {
  const icon = sound.muted ? '🔇' : '🔊';
  document.querySelectorAll('.btn-mute').forEach(btn => btn.textContent = icon);
}
```

- [ ] **Step 2: Replace the boot section at the bottom of ui.js**

Find the current two-line boot section:

```js
updateBalance();
renderBetting();
```

Replace it with:

```js
document.getElementById('gameMuteBtn').addEventListener('click', toggleMute);
updateBalance();
showTitleScreen();
```

- [ ] **Step 3: Browser test — title screen flow**

Open `index.html`. Verify:
- Title screen appears on load
- Clicking 🔊 toggles to 🔇 and back (both buttons update)
- Muting on title screen → reopening page → icon starts as 🔇 (localStorage persists)
- Clicking Play Now fades the title screen out and reveals the betting interface
- Ambient music starts on Play Now (quiet low hum — may need headphones)
- Muting during gameplay stops the ambient hum

- [ ] **Step 4: Commit**

```bash
git add ui.js
git commit -m "feat: add title screen logic, mute toggle, and ambient music wiring"
```

---

### Task 5: Add confetti burst and dealer reveal pulse

**Files:**
- Modify: `ui.js`

- [ ] **Step 1: Add `triggerConfetti` function to ui.js**

Add after `updateMuteButtons`:

```js
// ============================================================
// Confetti
// ============================================================
function triggerConfetti() {
  const colors = ['#c9a84c', '#27ae60', 'rgba(255,255,255,0.8)', '#e0b84d', '#2ecc71'];
  for (let i = 0; i < 60; i++) {
    const el = document.createElement('div');
    el.className = 'confetti-particle';
    el.style.left             = `${Math.random() * 100}vw`;
    el.style.background       = colors[Math.floor(Math.random() * colors.length)];
    el.style.animationDuration = `${1.4 + Math.random() * 0.8}s`;
    el.style.animationDelay   = `${Math.random() * 0.4}s`;
    document.body.appendChild(el);
    el.addEventListener('animationend', () => el.remove(), { once: true });
  }
}
```

- [ ] **Step 2: Call `triggerConfetti()` from `renderResult()`**

In `renderResult()`, find the block that sets `message` and `cls` (ends around the `else` clause). Immediately after that block and before the `resultOverlay.innerHTML` line, add:

```js
  if (cls === 'blackjack' || cls === 'win') triggerConfetti();
```

So the order becomes:

```js
  // ... message/cls block ...
  if (cls === 'blackjack' || cls === 'win') triggerConfetti();

  resultOverlay.innerHTML = `<div class="result-message ${cls}">${message}</div>`;
```

- [ ] **Step 3: Add dealer reveal pulse to `runDealerTurn()`**

In `runDealerTurn()`, find the first line after `actionBar.innerHTML = '';`. Insert the pulse immediately after:

```js
  const dealerArea = document.querySelector('.dealer-area');
  dealerArea.classList.add('dealer-area--reveal');
  dealerArea.addEventListener('animationend', () => {
    dealerArea.classList.remove('dealer-area--reveal');
  }, { once: true });
```

So `runDealerTurn` now starts:

```js
function runDealerTurn() {
  actionBar.innerHTML = '';

  const dealerArea = document.querySelector('.dealer-area');
  dealerArea.classList.add('dealer-area--reveal');
  dealerArea.addEventListener('animationend', () => {
    dealerArea.classList.remove('dealer-area--reveal');
  }, { once: true });

  const holeCardEl = dealerCardsEl.lastElementChild;
  // ... rest unchanged ...
```

- [ ] **Step 4: Browser test — confetti and reveal**

Open `index.html`, play through to a win:
- Win or blackjack → gold/green confetti rains down and disappears on its own
- Lose → no confetti
- Before dealer cards are revealed → dealer area briefly pulses/zooms slightly

- [ ] **Step 5: Commit**

```bash
git add ui.js
git commit -m "feat: add confetti burst on win and dealer reveal pulse"
```

---

### Task 6: Wire up sound calls

**Files:**
- Modify: `ui.js`

- [ ] **Step 1: Add chip sound in `addChip()`**

Find `function addChip(amount) {`. After `game.placeBet(amount);` add:

```js
  sound.playChip();
```

- [ ] **Step 2: Add deal sounds in `onDeal()`**

Inside `dealOrder.forEach((card, i) => {`, find where `container.appendChild(el)` is called. Add `sound.playDeal()` on the line after it:

```js
      container.appendChild(el);
      sound.playDeal();
```

- [ ] **Step 3: Add deal sound in `onHit()`**

Find `container.appendChild(el)` inside `onHit`. Add `sound.playDeal()` after it:

```js
  container.appendChild(el);
  sound.playDeal();
```

- [ ] **Step 4: Add deal sound in `onDouble()`**

Find `container.appendChild(el)` inside `onDouble`. Add `sound.playDeal()` after it:

```js
  container.appendChild(el);
  sound.playDeal();
```

- [ ] **Step 5: Add flip sound in `runDealerTurn()`**

Inside the `if (holeCardEl && holeCardEl.classList.contains('flipped'))` block, add `sound.playFlip()` as the first line:

```js
  if (holeCardEl && holeCardEl.classList.contains('flipped')) {
    sound.playFlip();
    const holeCard = game.dealerCards[1];
    // ... rest unchanged ...
```

- [ ] **Step 6: Add deal sound for each extra dealer card in `runDealerTurn()`**

Inside `extraCards.forEach(card => { setTimeout(() => {`, find `dealerCardsEl.appendChild(el)`. Add `sound.playDeal()` after it:

```js
      dealerCardsEl.appendChild(el);
      sound.playDeal();
```

- [ ] **Step 7: Add result sounds in `renderResult()`**

In `renderResult()`, after the `triggerConfetti()` call added in Task 5, add:

```js
  if (cls === 'blackjack') sound.playBlackjack();
  else if (cls === 'win')  sound.playWin();
  else if (cls === 'lose') sound.playLose();
  else if (cls === 'push') sound.playPush();
```

So the order is now:

```js
  if (cls === 'blackjack' || cls === 'win') triggerConfetti();

  if (cls === 'blackjack') sound.playBlackjack();
  else if (cls === 'win')  sound.playWin();
  else if (cls === 'lose') sound.playLose();
  else if (cls === 'push') sound.playPush();

  resultOverlay.innerHTML = `<div class="result-message ${cls}">${message}</div>`;
```

- [ ] **Step 8: Browser test — all sounds**

Play through a full game with sound on. Verify:
- Chip click → short tick on each chip press
- Deal → soft whoosh per card (4 sounds during initial deal)
- Hit/double → whoosh per card
- Dealer turn → flip swoosh on hole card reveal, then whoosh per extra card
- Win → 3-note ascending chime
- Blackjack → 4-note fanfare
- Lose/bust → descending tone
- Push → neutral mid tone
- Muting silences all of the above and fades ambient

- [ ] **Step 9: Commit**

```bash
git add ui.js
git commit -m "feat: wire sound calls to all game events"
```

---

### Task 7: Final verification and push

**Files:** None modified.

- [ ] **Step 1: Run the game.js test suite**

```bash
node tests/game.test.js
```

Expected: all 83 tests pass. If any fail, `game.js` was accidentally touched — check `git diff game.js`.

- [ ] **Step 2: Full browser playthrough**

Open `index.html` fresh (no cached state). Verify the complete flow:
1. Title screen appears — suit row, heading, tagline, Play Now button, mute toggle
2. Click 🔊 → becomes 🔇, ambient stays silent
3. Click 🔇 → becomes 🔊, ready to play with sound
4. Click Play Now → ambient starts, title fades out, betting screen appears
5. Place a bet → chip sounds
6. Deal → 4 card whooshes with staggered timing
7. Hit → card whoosh
8. Stand through to dealer turn → reveal pulse + flip swoosh + any extra card whooshes
9. Win → chime + confetti rain, fades cleanly
10. Blackjack → fanfare + confetti
11. Lose → descending tone, no confetti
12. Mute button in top bar works the same as on title screen
13. Reload page → mute preference preserved

- [ ] **Step 3: Push to remote**

```bash
git push
```
